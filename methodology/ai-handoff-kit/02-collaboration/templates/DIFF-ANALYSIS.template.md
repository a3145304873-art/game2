# 代码逻辑差异分析 · {标题}

> **用途**：基于当前 main 代码真实状态，说明要改哪里、为什么、有什么影响。模式 B 独有交付物，接收方写代码的"改动地图"。
> **产出方式**：**协作者手动撰写**（`/kickoff` 不产出）。基于 Step 3 的 Issue 分析 + SPEC/PLAN/TASKS。
> **红线**：必须引用真实代码路径；只分析不写代码；诚实写风险。

---

## 元信息

| 项 | 内容 |
|----|------|
| 对应 SPEC | `{N}-{slug}` |
| 分析基于的 main 版本 | {main 的 commit/日期，注明"分析时的代码状态"} |
| 撰写人 | {协作者} |
| 日期 | 2026-XX-XX |

---

## 1. 涉及功能与现状

> 需求涉及的功能，当前 main 代码如何实现。**必须引用具体文件路径、函数名、数据流**，证明读过代码。

### 1.1 涉及的代码区域

| 文件路径 | 关键函数/组件 | 当前职责 |
|---------|--------------|---------|
| `backend/xxx.py` | `func_a()` | {当前做什么} |
| `frontend/src/xxx.jsx` | `ComponentB` | {当前做什么} |

### 1.2 当前数据流（如涉及）

```
{当前 main 的数据流，用伪代码或文字描述}
用户输入 → backend/xxx.py:func() → ... → 前端渲染
```

### 1.3 当前已具备 / 缺失的能力

- **已具备**：{当前代码已经能做的}
- **缺失（本需求要补的）**：{当前代码做不到、本需求要加的}

---

## 2. 改动点清单

> 逐项列要新增/修改/删除什么。每项必须有具体文件路径 + 对应的 FR/Tasks 编号。

### 改动 1：{简述，如"新增输入字数限制"}
- **类型**：新增 / 修改 / 删除
- **文件**：`frontend/src/pages/ChatPage.jsx`、`frontend/src/components/ChatWindow.jsx`
- **改动说明**：{改什么、怎么改方向，不贴完整代码}
- **对应 FR**：FR-01
- **对应 Task**：T03

### 改动 2：{...}
- **类型**：...
- **文件**：`backend/builtins.py`
- **改动说明**：{...}
- **对应 FR**：FR-03
- **对应 Task**：T01

---

## 3. 数据与接口影响

> 涉及的 DB 表、接口、数据流变化。**源数据库只读——只能加新表/新库，不能改源表。**

| 类别 | 对象 | 变更 | 说明 |
|------|------|------|------|
| 数据库 | {表名} | 新增/不动 | {源 DB 只读，若要存数据需新库/新表} |
| 接口 | `{METHOD} /api/xxx` | 新增/修改 | {入参/出参变化} |
| 数据流 | {xxx → yyy} | 变更 | {变化点} |

---

## 4. 影响面分析

> 改动波及哪些现有功能、上下游。回归风险。

| 改动 | 影响的现有功能 | 回归风险 | 建议 |
|------|--------------|---------|------|
| 改动 1 | {现有功能} | 高/中/低 | {测试建议} |

---

## 5. 风险与约束

### 5.1 技术风险

| 风险 | 说明 | 应对建议 |
|------|------|---------|
| {风险} | {...} | {...} |

### 5.2 需遵守的项目约束（红线）

- [ ] 源数据库（`data/*.db`）只读打开（`mode=ro`），不改源表
- [ ] 正式服端口 3500/8500 不碰
- [ ] 不关闭非自己启动的进程（浏览器/编辑器）
- [ ] {本需求特有的约束}

### 5.3 需接收方确认的点

> 诚实标注不确定的地方，不要装懂。

- {问题 1}：{为什么不确定} → 建议接收方 {确认/决策}

---

## 6. 不改什么（Out of 改动范围）

> 与 SPEC 的 Out of Scope 呼应。明确哪些代码本次不碰。

- `{文件/模块}` —— 不改，{原因}
- `{文件/模块}` —— 不改，{原因}

---

## 自查（提交一致性审核前）

- [ ] 每个改动点都有**具体文件路径**（不是泛泛"修改后端"）
- [ ] 每个改动点都能追溯到 SPEC 的一条 FR
- [ ] 每个改动点都在 TASKS 有对应任务
- [ ] 没有改动点越界（不在 TASKS 范围内）
- [ ] 现状描述引用了真实的 main 代码（文件/函数）
- [ ] 风险诚实，不确定的已标注"需接收方确认"
- [ ] 源数据库只读等约束已遵守
- [ ] 只分析、未写业务代码
