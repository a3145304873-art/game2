# 控制映射设计参考

## 变量定义
- `关键动作` (List): 玩家克服挑战所需的主要操作
- `次要动作` (List): 移动视角、交互、聊天等非挑战性操作
- `输入设备` (Hardware): 传统控制器、体感设备(Wii/Kinect)等

## 体感设备限制
- 转身操作会导致玩家背对屏幕
- 空间要求：需要足够的活动空间
- 精度限制：某些操作难以精确控制

## 常见映射错误
- 将频繁操作映射到难以按下的按钮
- 同一功能在不同模式下使用不同按键
- 缺乏操作反馈导致玩家困惑